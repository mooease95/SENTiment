<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sentiment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sentimentlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sentimentlogo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InstantFeedback"></activity>
        <activity android:name=".ThreadMessages" />
        <activity android:name=".ThreadLists" />
        <activity android:name=".NewThread" />
        <activity android:name=".EmailAuth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- <action android:name="android.intent.action.VIEW" /> -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>